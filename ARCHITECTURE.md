## 📋 PART 5: Template ARCHITECTURE.md

```markdown
# Product Management System - Architecture

## C1: System Context Diagram

\`\`\`
┌─────────────────────────────────────────────────────┐
│                    System User                      │
│         (เจ้าของร้าน, พนักงาน, ผู้จัดการ)                 │
└────────────┬────────────────────────────────────────┘
             │ HTTP/JSON (CRUD Operations)
             ▼
┌─────────────────────────────────────────────────────┐
│       Product Management System                     │
│  • จัดการข้อมูลสินค้า (CRUD)                             │
│  • คำนวณมูลค่ารวมของสินค้า                              │
│  • กรองสินค้าตาม Category                             │
└────────────┬────────────────────────────────────────┘
             │ SQL Queries
             ▼
┌─────────────────────────────────────────────────────┐
│              SQLite Database                        │
│               (products.db)                         │
└─────────────────────────────────────────────────────┘
\`\`\`

### Actors
- **System User**: เจ้าของร้าน, พนักงาน, ผู้จัดการที่ใช้ระบบจัดการสินค้า

### System
- **Product Management System**: ระบบจัดการสินค้า
  - จัดการข้อมูลสินค้า (CRUD)
  - คำนวณมูลค่ารวม (price × stock)
  - กรองสินค้าตาม category

### External Systems
- **SQLite Database**: ฐานข้อมูลเก็บข้อมูลสินค้า

---

## C2: Container Diagram - Layered Architecture

\`\`\`
┌────────────────────────────────────────────────────────────────┐
│                        CLIENT (Browser)                        │
└────────────┬───────────────────────────────────────────────────┘
             │ HTTP/JSON
             ▼
╔════════════════════════════════════════════════════════════════╗
║                  PRODUCT MANAGEMENT SYSTEM                     ║
╠════════════════════════════════════════════════════════════════╣
║  ┌───────────────────────────────────────────────────────────┐ ║
║  │           📋 PRESENTATION LAYER                           │ ║
║  │  • Routes (productRoutes.js)                              │ ║
║  │  • Controllers (productController.js)                     │ ║
║  │  • Middlewares (errorHandler.js)                          │ ║
║  └──────────────────────┬────────────────────────────────────┘ ║
║                         │                                      ║
║                         ▼                                      ║
║  ┌───────────────────────────────────────────────────────────┐ ║
║  │           🧠 BUSINESS LOGIC LAYER                         │ ║
║  │  • Services (productService.js)                           │ ║
║  │  • Validators (productValidator.js)                       │ ║
║  │                                                           │ ║
║  │  Business Rules:                                          │ ║
║  │    ✓ Name, price, category required                       │ ║
║  │    ✓ Price must be > 0                                    │ ║
║  │    ✓ Calculate totalValue = Σ(price × stock)              │ ║
║  └──────────────────────┬────────────────────────────────────┘ ║
║                         │                                      ║
║                         ▼                                      ║
║  ┌───────────────────────────────────────────────────────────┐ ║
║  │           💾 DATA ACCESS LAYER                            │ ║
║  │  • Repositories (productRepository.js)                    │ ║
║  │  • Database (connection.js)                               │ ║
║  │                                                           │ ║
║  │  Methods:                                                 │ ║
║  │    • findAll(category)                                    │ ║
║  │    • findById(id)                                         │ ║
║  │    • create(productData)                                  │ ║
║  │    • update(id, productData)                              │ ║
║  │    • delete(id)                                           │ ║
║  └──────────────────────┬────────────────────────────────────┘ ║
╚═════════════════════════╪══════════════════════════════════════╝
                          │ SQL
                          ▼
              ┌─────────────────────────┐
              │    SQLite Database      │
              │     (products.db)       │
              │                         │
              │  Table: products        │
              │  - id                   │
              │  - name                 │
              │  - price                │
              │  - stock                │
              │  - category             │
              │  - created_at           │
              └─────────────────────────┘
\`\`\`
Layer Responsibilities
1. Presentation Layer

หน้าที่

รับ HTTP Request

Parse parameters / body

เรียก Service

ส่ง HTTP Response (JSON)

ไฟล์

studentRoutes.js

studentController.js

errorHandler.js

2. Business Logic Layer

หน้าที่

Validation ข้อมูลนักศึกษา

ตรวจ Business Rules

ประมวลผล Logic

เรียก Repository

ไฟล์

studentService.js

studentValidator.js

Business Rules

name, student_id, major ต้องไม่ว่าง

student_id ต้องไม่ซ้ำ

age ต้องมากกว่า 0

3. Data Access Layer

หน้าที่

ติดต่อฐานข้อมูล

CRUD Operations

Execute SQL

ส่งข้อมูลกลับให้ Service

ไฟล์

studentRepository.js

connection.js

Methods

findAll()

findById(id)

create(studentData)

update(id, data)

delete(id)

Data Flow: Create Student
Client → Controller → Service → Repository → Database
         ↓ parse     ↓ validate  ↓ SQL       ↓ insert
         ← response ← ← ← ← ← ← ← ← ← ←


Steps

Client ส่ง POST /students

Controller แปลง request → เรียก Service

Service ตรวจสอบข้อมูล (Validation)

Repository execute SQL INSERT

Database บันทึกข้อมูล

Response กลับไปยัง Client

Summary

Architecture Benefits

Separation of Concerns

Maintainability

Testability

Scalability

Key Principles

แยกความรับผิดชอบชัดเจน

Dependency ไหลจากบนลงล่าง

ห้ามข้าม Layer

Business Rules อยู่ที่ Business Layer เท่านั้น


1. เปิด GitHub repository
# 2. ตรวจสอบว่าไฟล์ครบถ้วน:
#    ✅ src/ folder (ทั้ง 3 layers)
#    ✅ server.js
#    ✅ package.json
#    ✅ README.md
#    ✅ ARCHITECTURE.md
#    ✅ .gitignore